server:
  port: 8082

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/account-db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    root: INFO
    com.banking.accountservice: DEBUG