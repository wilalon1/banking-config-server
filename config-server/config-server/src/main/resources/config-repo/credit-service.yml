server:
  port: 8083

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/credit-db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    root: INFO
    com.banking.creditservice: DEBUG